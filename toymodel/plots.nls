to updatePlots 
  
  let l_surf_parcelle []
  let dada [parcelle-id] of patches
  

  set-current-plot "patchs par cuisine"
  ;;pour réaliser un histogram on a besoin d'une liste
  set-plot-x-range 0 ( (max dada) + 5)
  ;  set-histogram-num-bars 8
  histogram dada
  let maxbar modes dada
  show maxbar
  let maxrange 20;length filter [ ? = item 0 maxbar ] dada
  set-plot-y-range 0 max list 10 maxrange
  set-plot-y-range 0 1000
  
end